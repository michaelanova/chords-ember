<h1 class="main-name">*</h1>
{{main-menu}}
<div class="main">
  {{#if session.isAuthenticated}}
  Logged in as {{session.currentUser.displayName}}
  <button {{action "signOut"}}>Sign out</button>
  {{outlet}}
{{else}}
  <button {{action "signIn" "facebook"}}>Sign in with FB</button>
{{/if}}
  {{outlet}}
</div>
