<div class="heading">
  Kategorie článků
</div>
<div class="category-list">
  {{#each model as |category|}}
    <div class="list bg">
      {{#link-to "articles.category.show" category.id}}
      <div class="info">
        <div class="name">
          {{category.name}}
        </div>
        <!--div class="count">
          {{!category.count}}
        </div-->
      </div>
      {{/link-to}}
    </div>
  {{/each}}

</div>
